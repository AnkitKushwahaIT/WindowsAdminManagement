# Security Guidance

Never commit real passwords, API keys, access tokens, certificates/private keys, tenant credentials, or other secrets to this public repository.

A shared static local administrator password is suitable only as an example or controlled temporary mechanism. Managed enterprise Windows devices should use Windows LAPS or another per-device credential management approach.
